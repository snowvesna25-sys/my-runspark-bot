services:
  - type: web
    name: runspark-bot
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python runspark_bot.py"
    envVars:
      - key: BOT_TOKEN
        sync: false